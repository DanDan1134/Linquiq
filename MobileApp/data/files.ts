/**
 * Files Data
 *
 * Mock data array containing sample files with:
 * - File metadata (name, date, type, number)
 * - Creation details (timestamp, creator email)
 * - URLs and content for each file
 * - Color coding for different file types
 */

export const files = [] as const;
